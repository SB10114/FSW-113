class Person{

   constructor(){ 
        first= first
        last= last     
}

    fullName ( ) {
             return( `The full name of the person = ${last}, ${first}`)

    }

}

export default Person